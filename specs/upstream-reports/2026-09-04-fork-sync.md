# Fork Sync Report — 2026-09-04 (batch 224 / fork sync)

## Summary

- **Upstream commits pulled**: 0 — `upstream/main` is `b1a93688d3c`, exactly the commit the rolling branch
  was already level with after batch 223. Nothing new was published upstream this cycle.
- **Fork commits synced**: 2 (`integratedForkHead` `1f5e270bd14` → `b03a84045f9`)
- **Conflicts resolved**: 27 files (4 on #1056, 23 on #843)
- **Post-merge repairs beyond the conflicts**: 11 further defects, none of which conflicted
- **Risk level**: MEDIUM — a 133-file / 12k-insertion fork feature landing on a branch that carries the
  Option-M `personId` → `personGroupId` re-key and the relocated config + Drift layers
- **Recommendation**: PROCEED (all local gates green; full CI dispatch pending)

This was a **pure fork sync**, not an upstream batch. The per-batch product-direction gate did not fire —
there were no upstream commits to gate.

## Incoming fork commits

| SHA (on `origin/main`) | Replayed as   | Summary                                                                              | Footprint                                                       |
| ---------------------- | ------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| `7d4f7c45e4a`          | `0f6902828d9` | fix: send "view in timeline" on a memory's Space photo to that Space (#1047) (#1056) | 16 files, mobile + web                                          |
| `b03a84045f9`          | (final)       | feat: individual pet recognition (Phase 2) (#843)                                    | 133 files, 12,003 insertions — server, ML, web, e2e, docs, i18n |

## The governing hazard: rolling's vocabulary is ahead of `main`'s

Both commits were authored and CI-verified against `origin/main`. The rolling branch carries three
divergences `main` does not, and **every one of them produced breakage that git merged without a
conflict**:

1. **Option M re-key** — `person` is keyed `(ownerId, personGroupId)` with **no `id` column**, and
   `asset_face` has `personGroupId`, not `personId`.
2. **Config relocation** — `server/src/config.ts`, `dtos/system-config.dto.ts` and
   `dtos/model-config.dto.ts` are **deleted**; config is zod schemas in `dtos/config.dto.ts` +
   `gallery/config.dto.ts`, and the e2e config DTO is `adminConfigDto`, not `systemConfigDto`.
3. **Mobile Drift relocation (immich-30966) + build-time codegen (#888)** — `DriftDatabaseRepository`
   is gone, the db lives at `data/db/main/database.dart`, and `mobile/openapi/**` was deleted in favour
   of generated `mobile/generated/openapi/`.

## Conflict resolutions

### #1056 — 4 conflicts

#### `mobile/lib/presentation/widgets/timeline/scroll_drain.dart`

- **Fork side (rolling)**: `if (!hasPending) { return ...; }` — braced.
- **Upstream side (`main`)**: one-liner `if (!hasPending) return ...;` with the new
  `|| pendingSpaceId != timelineSpaceId` condition.
- **Resolution**: rolling's braced form **with** the new condition.
- **Why it matters**: rolling enables `always_put_control_body_on_new_line: warning`, which
  `dart analyze --fatal-infos` treats as an error. Taking `main`'s one-liner would have reddened
  Static Code Analysis, Unit Test Mobile and Gallery Build Mobile together.
- **Risk**: LOW. Verified by `dart analyze --fatal-infos` (0 issues).

#### `mobile/lib/presentation/widgets/memory/memory_bottom_info.widget.dart`

- **Fork side**: imports `generated/translations.g.dart` (rolling is off easy_localization; call sites
  are `context.t.<key>`, not `'<key>'.tr()`).
- **Upstream side**: swaps `view_in_timeline_action.dart` → `view_in_timeline_destination.dart`.
- **Resolution**: keep rolling's translations import, take the new destination import. The
  `viewMemoryAssetInTimeline` call body merged cleanly and already carries `context.t.view_in_timeline`.
- **Risk**: LOW.

#### `MemoryViewer.svelte` (path renamed by upstream: `memory/` → `memories/[id]/`)

- **Fork side**: rolling adopted upstream's memory viewer — href-based
  `handleNavigate(href, options)`, and no `asHref` / `memoryViewerSource` / `exitRoute`.
- **Upstream side (`main`)**: still the pre-adoption `handleNavigate(asset, memory)` shape, plus the
  genuinely new `viewInTimelineHref` helper and the `AssetResponseDto` import.
- **Resolution**: rolling's structure **plus** only the three real additions. `main`'s
  `asHref`/`memoryViewerSource`/`exitRoute` are the _old_ design and were deliberately not resurrected.
- **Verification**: whole-file diff against rolling's HEAD shows exactly three hunks (import,
  helper, anchor) and an orphan-symbol grep returns nothing.
- **Risk**: MEDIUM at resolution time, now LOW — see the red-proof below.

#### `MemoryViewer.spec.ts`

- **Shape K asymmetry**: `ours` was **0 lines** against `base` 30 and `theirs` 103 — rolling had
  deleted the standalone `MemoryViewer GalleryViewer grouping` describe (it was folded into the
  `#790` describe by `e3e36bb1535`), while `main` still carried it plus the new `#1047` describe.
- **Resolution**: keep rolling's deletion; add only the `#1047` describe — **rewritten** onto rolling's
  harness. `main`'s version uses `mockMemoryManager` + `mockPage.url =` + `mockAfterNavigate`, none of
  which exist here; rolling uses the real `memoryManager` singleton and `mockPage.reset(url, {routeId, params})`.
  Taking it verbatim would not have compiled.
- **Risk**: LOW — and the tests are load-bearing (below).

### #843 — 23 conflicts

| File(s)                                                                                                                 | Resolution                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mobile/openapi/**` (8, delete/modify)                                                                                  | Keep rolling's deletion — `1ed55096add` moved the Dart client to build-time codegen and deleted all 552 files.                                                                                                                                                                                                              |
| `server/src/config.ts`, `dtos/model-config.dto.ts`, `dtos/system-config.dto.ts` (delete/modify)                         | Keep rolling's deletion; **port** #843's additions to `gallery/config.dto.ts` (new `GalleryPetRecognitionSchema`, `petRecognition` default) and `dtos/config.dto.ts` (schema wiring, `clusterNewPets` nightly task, defaults). Bounds and the `minFaces ≤ 1000` rationale carried over verbatim from `model-config.dto.ts`. |
| `server/src/repositories/person.repository.ts` (6 hunks)                                                                | Merged both sides; **hunk 3 was another Shape K asymmetry** (ours 19 lines / base 0 / theirs 1) — resolved as rolling's `clusterGroupId` block _plus_ theirs' `excludePetFaces` `$if`. `purgePetRecognitionArtifacts` and `getPetFaceForRecognition` re-keyed to `personGroupId`.                                           |
| `server/src/services/pet-detection.service.ts` (3 hunks)                                                                | Took #843's **whole restructured file** (recognizable/bucketed split, mid-flight model-switch guard, extracted `writeDetectedPetsAsSpeciesBuckets`) and re-applied rolling's re-key on top. Verified: the resulting diff vs #843 is _only_ the re-key.                                                                      |
| `server/src/services/person.service.ts` (3)                                                                             | Merged rolling's `clusterGroupId` / `personGroupId` with #843's `excludePetFaces` on both reset arms.                                                                                                                                                                                                                       |
| `server/src/repositories/asset-job.repository.ts`                                                                       | Rolling's `previewFile` selection + #843's inline faces-with-`isPet` select. Confirmed `withFaces(eb, true, true)` applies no filters, so the inline form is exactly equivalent plus `isPet`.                                                                                                                               |
| `server/test/mappers.ts`                                                                                                | #843's `isPet: false` default + rolling's `previewFile` mapping.                                                                                                                                                                                                                                                            |
| `machine-learning/.../pet_detection/detection.py`                                                                       | Rolling's `decode_pil`/`pil_to_cv2` import + #843's `ModelFormat`. Both confirmed by usage.                                                                                                                                                                                                                                 |
| `e2e/mise.toml`                                                                                                         | Kept upstream's (rolling's) `--filter immich-e2e...`; #843's quoted variant is functionally identical, and the `...` intent it documents is already satisfied. Avoids gratuitous divergence in an upstream-owned file.                                                                                                      |
| `person.service.spec.ts`, `MachineLearningSettings.spec.ts`                                                             | Merged both intents (`excludePetFaces` expectations + rolling's `personGroupId` / `AdminConfigDto`).                                                                                                                                                                                                                        |
| `open-api/immich-openapi-specs.json`, `packages/sdk/src/fetch-client.ts`, `server/src/queries/asset.job.repository.sql` | Resolved to rolling's copy and then **regenerated** — see below.                                                                                                                                                                                                                                                            |

## Zero-conflict breakage found after the merge

Eleven defects merged cleanly and were caught only by running gates. **This is the bulk of the cycle's
real work.**

| #   | Defect                                                                                                                                                              | Caught by                                 | Class                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ----------------------------------- |
| 1   | 168 type errors across pet code — `person.id` / `asset_face.personId` throughout                                                                                    | `server pnpm check`                       | Re-key                              |
| 2   | `petFacePredicate` itself joined `person.id` = `asset_face.personId`                                                                                                | `server pnpm check`                       | Re-key                              |
| 3   | `searchPets` selected `asset_face.personId`                                                                                                                         | `server pnpm check`                       | Re-key                              |
| 4   | `pet-recognition.service.ts` imported the deleted `src/config`                                                                                                      | `server pnpm check`                       | Config relocation                   |
| 5   | `pet-detection.service.spec.ts` mocked `person.create` while the service calls `createWithGroup` → 5 tests returned `failed`                                        | server unit suite                         | Re-key (mock arity is untyped)      |
| 6   | `person.service.spec.ts` asserted `getAllFaces({personId: null})`                                                                                                   | server unit suite                         | Re-key (assertion literal, untyped) |
| 7   | `pet-detection.service.spec.ts:588` asserted `face.personId === undefined` — **vacuously true** after the re-key                                                    | manual grep for residual `personId`       | Assertion that cannot fail          |
| 8   | `mobile/openapi/lib/model/pet_recognition_config.dart` — #843 **added** one generated file into a directory rolling had deleted; git auto-added it with no conflict | `dart analyze --fatal-infos`              | **Shape I, inverted**               |
| 9   | `mobile/.../shared_space.repository.dart` extended `DriftDatabaseRepository` and imported the pre-relocation `db.repository.dart`                                   | `dart analyze --fatal-infos`              | **Shape L**                         |
| 10  | `machine-learning/.../pet_recognition/recognition.py` imported `decode_cv2`, removed on rolling                                                                     | `mypy --strict` (ruff did **not** see it) | Shared-dependency drift             |
| 11  | `machine-learning/test_main.py` used `cv2` with no `import cv2` — rolling had dropped the import                                                                    | `pytest` collection                       | Shared-dependency drift             |
| 12  | 6 e2e errors: `systemConfigDto` → `adminConfigDto`, `createFace({personId})` → `personGroupId`                                                                      | `cd e2e && pnpm check`                    | Config relocation + re-key          |

Notes worth carrying forward:

- **#8 is a new variant of Shape I.** The recorded shape is "fork deletes a file, upstream later creates
  one at that path". Here a **fork** commit added a file into a directory another **fork** commit had
  deleted. The fork-owned-file audit passes (the path is not fork-owned), nothing conflicts, and only
  `dart analyze` sees it. The file was also wrong twice over: it declared `PetRecognitionConfig`, a
  schema name that does not exist on rolling (rolling's config port names it
  `AdminConfigPetRecognitionDto`, generated at `mobile/generated/openapi/lib/model/admin_config_pet_recognition_dto.dart`).
- **#10 shows `ruff` is not a substitute for `mypy`** — `ruff check` passed clean on a module importing a
  symbol that does not exist. Only `mypy --strict` resolved it.
- **#7 is the "assertion that cannot fail" trap.** After a re-key, an assertion that a now-nonexistent
  property is `undefined` passes forever. Grepping for residual old-vocabulary tokens after tsc goes
  green is what found it; tsc never will.

## Generated-artifact regeneration

Both generators were re-run and their output committed, per the standing IOU rule:

- `mise run //:open-api` → `open-api/immich-openapi-specs.json` (+49) and
  `packages/sdk/src/fetch-client.ts` (+17): `AdminConfigPetRecognitionDto`, `clusterNewPets`, the
  `PetRecognition` queue/job names. Proven at the regenerated state by a second run producing **zero**
  drift.
- `mise run //:sql` against a fresh pinned Postgres → `asset.job.repository.sql`,
  `person.repository.sql`, `search.repository.sql`. The drift is exactly the `personGroupId` re-key
  plus the new `isPet` predicate. **Block-survival check: 29/29, 45/45, 53/53 headers — none lost.**

> Trap hit and worth recording: the first `pnpm migrations:run` silently connected to `localhost:5432`
> (`DB_URL` is what `sql-tools` reads, not the `DB_*` vars) and half-applied, which produced a
> plausible-but-wrong 14-file SQL diff. The tell was `relation "workflow" does not exist` in the
> generator output. Reverted, set `DB_URL` explicitly, re-ran — 3 files, all correct.

## Fork feature verification

| Feature                          | Status | Notes                                                                                                       |
| -------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------- |
| Shared Spaces                    | OK     | `ci-invariants-check` green incl. `person-join-not-viewer-filtered`; shared-space medium + unit specs pass. |
| Storage Migration                | OK     | Untouched this cycle.                                                                                       |
| Pet Detection                    | OK     | 37 unit tests green after re-keying the `create` → `createWithGroup` mocks.                                 |
| Pet Recognition (new)            | OK     | 61 unit + 46 medium + 8 ML tests green.                                                                     |
| Image Editing                    | OK     | `previewFile` selection preserved through the `asset-job.repository.ts` conflict.                           |
| Branding                         | OK     | `branding/` tree byte-identical to the last green tip.                                                      |
| Google Photos Import             | OK     | Untouched.                                                                                                  |
| Search V3 coexistence            | OK     | `search-v3-not-dispatched` invariant green.                                                                 |
| Memory viewer / view-in-timeline | OK     | 4 new `#1047` web tests, proven red without the fix.                                                        |

## Test honesty

The `#1047` web tests were proven load-bearing rather than assumed: reverting `viewInTimelineHref` to
the unconditional `Route.photos({at})` turns exactly the two Space cases red (2 failed / 9 passed), and
restoring it returns 11/11. The mobile `SharedSpaceRepository` tests (46 assertions over
`findSpaceIdForAsset`) pass against the **adapted** relocated-Drift form, so that repair is verified
behaviourally, not just by compilation.

## Local CI verification

| Check                                             | Status | Notes                                                                                                                                                        |
| ------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `server pnpm build` (+ postbuild migration sync)  | PASS   | Synced 64 Gallery migrations, 1 compatibility alias                                                                                                          |
| `server pnpm check` (tsc)                         | PASS   | from 168 errors → 0                                                                                                                                          |
| `server pnpm lint`                                | PASS   | after removing two now-unused `Mocked` imports                                                                                                               |
| `server prettier --check`                         | PASS   |                                                                                                                                                              |
| Server unit tests                                 | PASS   | 6296 passed, 12 skipped (201 files)                                                                                                                          |
| Server medium (pet + person)                      | PASS   | 46 + 42 tests                                                                                                                                                |
| `web check:typescript`                            | PASS   |                                                                                                                                                              |
| `web check:svelte`                                | PASS   | 632 files, 0 errors                                                                                                                                          |
| Web unit tests                                    | PASS   | 6147 passed (380 files)                                                                                                                                      |
| `web prettier --check`                            | PASS   |                                                                                                                                                              |
| `e2e pnpm check` / `lint` / `format`              | PASS   | 6 errors fixed first                                                                                                                                         |
| `dart analyze --fatal-infos`                      | PASS   | 0 issues (from 2335, then 4)                                                                                                                                 |
| `dart format --set-exit-if-changed`               | PASS   | 865 files, 0 changed                                                                                                                                         |
| `flutter test` (3.47.1)                           | PASS   | 3508 passed                                                                                                                                                  |
| ML `ruff format` / `ruff check` / `mypy --strict` | PASS   | mypy found defect #10                                                                                                                                        |
| ML `pytest`                                       | PASS   | 132 passed, 3 skipped                                                                                                                                        |
| ML `pet-recognition-training` pytest              | PASS   | 63 passed                                                                                                                                                    |
| `.github` prettier                                | PASS   |                                                                                                                                                              |
| `i18n` prettier                                   | PASS   |                                                                                                                                                              |
| `make commit-autolink-check`                      | PASS   | 1437 messages scanned, ceiling 1064                                                                                                                          |
| `make fork-patches-check`                         | PASS   |                                                                                                                                                              |
| `make ci-invariants-check`                        | PASS   | 5/5 invariants                                                                                                                                               |
| `make mobile-drift-rebase-check`                  | PASS   | schemaVersion/snapshots/callbacks consistent                                                                                                                 |
| `make upstream-postrebase-audit`                  | PASS*  | Generated Query Block Survival OK; the only ISSUE is the informational Generated Artifact Review list, and the tree is proven to be at the regenerated state |
| `revert-to-immich.sql` coverage                   | PASS   | both new pet migrations present; detector reports no gaps                                                                                                    |

## Inconsistencies found

Beyond the twelve defects above, none. Two deliberate divergences were recorded rather than "fixed":

- `e2e/mise.toml` keeps upstream's unquoted `--filter immich-e2e...` rather than #843's quoted variant.
- `pet-search.repository.spec.ts`'s helper parameter was renamed `personId` → `personGroupId` for
  vocabulary consistency even though the original compiled (it mapped correctly at the call site).

`shared_space_person_face.personId` is a genuinely different column and was **not** re-keyed.

### Pre-existing on `main` — needs its own PR there, deliberately NOT fixed here

`docs/docs/install/config-file.md`'s `nightlyTasks` JSON dump lists `clusterNewFaces` but **not**
`clusterNewPets`, even though #843 added that key to the config. The block is hand-maintained (no
generator, no CI check), and `origin/main` has the same omission — so this is a gap in #843 as merged,
not something this sync introduced. Fixing it here would diverge rolling from `main` for a reason
unrelated to the sync; it belongs in a follow-up on `main`. The defaults that _are_ documented
(`enabled: false`, `modelName: "pet-recognition-base"`, `maxDistance: 0.55`, `minFaces: 1`) match the
ported `galleryMachineLearningDefaults.petRecognition` exactly.

## Remote CI Verification

- **Test branch**: `rebase/upstream-batch-224`
- **Commit validated**: `16aa0c2c23f` — **10/10 green on the first pass, no re-runs, no flakes.**
  The report commit was later amended to add the "pre-existing on `main`" note above; every code
  directory (`server web mobile machine-learning e2e open-api packages i18n .github docker deployment
branding scripts docs tools`) is byte-identical between `16aa0c2c23f` and the final tip, so the
  validation carries.

| Workflow                                  | Status | Run         |
| ----------------------------------------- | ------ | ----------- |
| `test.yml`                                | GREEN  | 33872763138 |
| `docker.yml`                              | GREEN  | 33872457032 |
| `static_analysis.yml`                     | GREEN  | 33872239213 |
| `gallery-build-mobile.yml`                | GREEN  | 33872246751 |
| `gallery-mobile-smoke.yml`                | GREEN  | 33872241333 |
| `gallery-ml-smoke.yml`                    | GREEN  | 33872622722 |
| `gallery-rebase-smoke.yml`                | GREEN  | 33872244090 |
| `gallery-revert-to-immich-validation.yml` | GREEN  | 33872462519 |
| `storage-migration-tests.yml`             | GREEN  | 33872459945 |
| `storage-migration-e2e.yml`               | GREEN  | 33872467397 |

- **Failures fixed**: none — nothing failed remotely. Every defect was caught by a local gate first.
- **Confirmed flakes**: none.
- **Process note**: six workflows were briefly double-dispatched (a backgrounded wave-2 dispatch had
  buffered its stdout, so it looked idle and was re-issued). The duplicates were cancelled; that left
  `Test` with no live run, because its concurrency group had already cancelled the older one — it was
  re-dispatched. Read a backgrounded dispatcher's exit, not its interim output.

## Landing

Not applicable. `upstream/main` has published no new tag (latest final release remains `v3.1.0`;
`v3.2.0-rc.0/1/2` are pre-releases). The branch stays off `main`, per the standing rule.
