# Fork Sync Report — 2026-09-05 (no upstream batch)

## Summary

- **Upstream commits pulled**: 0 — the rolling branch was already level with `upstream/main` (`4c7b30c18b5`) at the end of batch 226, and upstream has not moved since.
- **Fork commits synced**: 5 (`d26e6523d16..3bb200f4364` on `origin/main`)
- **Conflicts resolved**: 1 (`i18n/it.json`)
- **Risk level**: LOW
- **Recommendation**: PROCEED

This cycle consumed no upstream batch, so the per-batch product-direction gate did not apply. The work
is a pure fork sync: five web/docs PRs that merged to `main` while the rolling branch sat off it.

## Incoming Fork Changes

| SHA (on `main`) | Summary                                                                                  | Area       | Risk to Rolling                                                                                    | Notes                                                                                                               |
| --------------- | ---------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `525e927deef`   | fix(web): apply the persisted People/Pets filter on the first load (#1071) (#1072)       | web        | MEDIUM — touches the People + Space People pages, the surface #1070 re-keyed on rolling last cycle | New fork-only `web/src/lib/utils/people-filter.ts`; both `+page.ts` loaders now issue the filtered request up front |
| `7ceb53bb54b`   | docs: document nested folders for space albums (#1074)                                   | docs       | LOW                                                                                                | New `docs/docs/features/space-album-folders.md` + two cross-links                                                   |
| `7b3f001d14d`   | fix(web): keep the People filter reachable when it matches nobody (#1073)                | web + i18n | MEDIUM — edits all ten gated locales                                                               | Adds `search_no_pets`; the only conflict of the cycle                                                               |
| `6fd2d7c03bc`   | feat(web): make the search mode visible and tappable next to the search bar (#1075)      | web        | LOW                                                                                                | New fork-only `search-mode-control.svelte`; moves mode UI out of `global-search-footer.svelte`                      |
| `3bb200f4364`   | fix(web): give the content panel its chrome gutter when the sidebar is collapsed (#1076) | web        | LOW                                                                                                | `web/src/styles/gallery-theme.css` only — spans the Compact Sidebar Rail and Token-Driven Re-skin fork surfaces     |

## Sync Mechanics

`make upstream-sync-fork-main` **threw** on `#1073` and **rolled back cleanly** — `HEAD` unchanged at
`b41e43b98df`, no `CHERRY_PICK_HEAD`, clean tree. (Batch 226 saw the script behave both ways in a
single cycle, so the `git rev-parse -q --verify CHERRY_PICK_HEAD` check was run before deciding state,
per the skill.) The five commits were then hand-applied with `git cherry-pick --ff` in `main` order,
and `integratedForkHead` was advanced by hand to `3bb200f4364` with an `appendHistory` entry recording
the manual application. `make upstream-rolling-status` now reports **0 fork commits pending**.

## Conflict Resolutions

### Conflict: `i18n/it.json`

- **Fork side (rolling)**: `"search_no_people_named": "Nessuna persona chiamata \"{name}\""` — rolling
  carries a grammar fix (`chiamate` → `chiamata`) that `main` does not.
- **Upstream side (`#1073`)**: keeps the old `chiamate` spelling and inserts the adjacent new key
  `"search_no_pets": "Nessun animale domestico"`.
- **Resolution**: kept rolling's corrected string **and** added the new key. The two changes are
  independent; the conflict is purely positional (adjacent lines in an alphabetically sorted file).
- **Risk**: LOW.
- **Verification**: `python3 -c "json.load(...)"` on the resolved file, `search_no_pets` confirmed
  present in all ten gated locales, and `npx prettier --check i18n/*.json` clean.

## Zero-Conflict Semantic Break Gate

Every synced file was diffed against `origin/main` after the cherry-picks. **All source files are
byte-identical to `main`**; the only deltas are:

- the ten `i18n/*.json` files (rolling legitimately carries many fork-only keys `main` lacks), and
- `web/src/routes/(user)/people/+page.svelte`, which differs from `main` by exactly one pre-existing
  rolling-only lint fix (`==` → `===`).

The whole-cycle tree diff (`b41e43b98df..HEAD`) is 30 files / +1117 / −229, and every file is accounted
for by one of the five PRs — no unexplained loss.

Shape-specific checks run:

| Shape                                       | Check                                                                                                            | Result                                                                                            |
| ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| Q (fork PR lands in a retired directory)    | `git ls-tree -r --name-only HEAD -- mobile/openapi \| wc -l`                                                     | 0 tracked — clean (none of these PRs touch mobile)                                                |
| — (zero-byte file from `checkout --theirs`) | scan for empty tracked files                                                                                     | only the three known-legitimate ones (`CODEOWNERS`, `docs/static/.nojekyll`, `docs/static/CNAME`) |
| L (import resolution after a tree change)   | covered by `tsc --noEmit` + `svelte-check` for web; no mobile files changed, so the Dart sweep is not applicable | green                                                                                             |
| — (branding i18n override gap)              | new `en.json` values naming the upstream product with no override                                                | none                                                                                              |

**Local-gate trap worth recording (not a branch defect):** `#1072`'s new
`spaces/[spaceId]/people/page-load.spec.ts` mocks `getSpacePeople` / `getSpacePeopleStatistics` with the
`$type` query param that `#1070` added last cycle. `pnpm check:typescript` reported two `TS2345` errors
because this worktree's `packages/sdk/build` predated `#1070`'s SDK regeneration — the SDK **source**
(`packages/sdk/src/fetch-client.ts`) already had `$type`. `packages/sdk/build` is gitignored and
untracked, and CI rebuilds it, so **CI would not have failed on this**. Fixed by `mise run //:sdk:build`;
no code was changed. This is the recurring stale-SDK-build trap, now with a new trigger: a fork PR whose
spec consumes an SDK symbol a _previous_ cycle's fork PR introduced.

## Fork Feature Verification

| Feature                                                      | Status | Notes                                                                                                      |
| ------------------------------------------------------------ | ------ | ---------------------------------------------------------------------------------------------------------- |
| Shared Spaces                                                | OK     | Space People page loader changes land intact; `space-people-page.spec.ts` green                            |
| Space People Type Filter (#1065/#1070)                       | OK     | Persisted All/People/Pets filter now applied on first load; the "matches nobody" escape hatch is reachable |
| Global Search / Command Palette                              | OK     | Search-mode control extracted to its own component; footer + palette specs green                           |
| Web Compact Sidebar Rail                                     | OK     | Collapsed-rail chrome gutter added in `gallery-theme.css`; `gallery-theme.spec.ts` green                   |
| Web Token-Driven Re-skin                                     | OK     | Change is token/CSS-layer only, no component edits                                                         |
| Search V3 coexistence                                        | OK     | `search-v3-not-dispatched` CI invariant passed                                                             |
| Storage Migration / Pet Detection / Image Editing / Branding | OK     | Trees byte-identical to the last 10/10-green tip                                                           |

## CI and Infrastructure Verification

| Check                                          | Status | Notes                                                                                           |
| ---------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------- |
| `make commit-autolink-check`                   | OK     | 1464 messages scanned, fork PR ceiling 1076, no cross-repo autolink                             |
| `make fork-patches-check`                      | OK     | `@immich/ui` patch metadata consistent                                                          |
| `make ci-invariants-check`                     | OK     | 5/5 invariants pass, including `search-v3-not-dispatched` and `person-join-not-viewer-filtered` |
| Workflow files / Docker image names / branding | OK     | `.github`, `branding`, `docker` trees byte-identical to the last green tip                      |

## Database Migration Analysis

**Not applicable.** `server/` is byte-identical to the last CI-green tip (`77711f5dade`); no migration,
schema table or repository file changed this cycle. `scripts/revert-to-immich.sql` therefore needs no
new entries.

## Mobile Drift Migration Analysis

**Not applicable.** `mobile/` is byte-identical to the last CI-green tip. `schemaVersion` and the
`drift_schemas/main/` snapshots are untouched.

## Inconsistencies Found

None. The only conflict was the `i18n/it.json` adjacency described above.

## Pattern Propagation

No broad refactors incoming — there were no upstream commits this cycle.

## Local CI Verification

Scoped by tree identity against the last 10/10-green tip `77711f5dade`. **`server`,
`machine-learning`, `mobile`, `e2e`, `open-api`, `packages`, `.github`, `docker`, `deployment`,
`branding`, `tools`, `scripts` are all byte-identical** — only `web`, `i18n` and `docs` changed, so only
their gates were run.

| Check                                                   | Status                                          | Notes                                                        |
| ------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------ |
| `mise run //:sdk:build`                                 | PASS                                            | Required — the worktree's SDK build was stale (see above)    |
| `web pnpm check:typescript`                             | PASS                                            | exit 0                                                       |
| `web pnpm check:svelte`                                 | PASS                                            | 640 files, 0 errors, 0 warnings                              |
| `web npx vitest --run`                                  | PASS                                            | 390 files passed / 1 skipped; 6385 tests passed              |
| `web npx eslint . --rule '{"tscompat/tscompat":"off"}'` | see CI results section                          | run locally; authoritative result is CI's Lint Web           |
| `pnpm format:fix` + i18n drift check                    | PASS                                            | zero changed files — matches the `Test i18n` job's assertion |
| `npx prettier --check docs/...`                         | PASS                                            | all three changed docs files                                 |
| Server / ML / mobile / e2e gates                        | SKIPPED — trees identical to the last green tip | stated rather than skipped silently                          |

## Remote CI Verification

- **Test branch**: `rebase/upstream-rolling-v3.1.1`
- **Commit validated**: recorded in the follow-up commit

_(Filled in after dispatch.)_

## Post-Sync Verification

- Commits behind `upstream/main`: **0**
- Fork commits pending: **0**
- `integratedForkHead`: `3bb200f4364`
- Upstream latest **final** tag: still **v3.1.0** (`v3.2.0-rc.0..rc.3` exist but are release candidates),
  and `branding/config.json` already pins `3.1.0` — so the standing landing rule is not satisfied and the
  branch **stays off `main`**.
